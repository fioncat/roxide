clone = "git.mydomain.com"
icon = "M"

[api]
provider = "gitlab"
token = "${TEST_GITLAB_TOKEN}"
cache_hours = 50
host = "api.git.mydomain.com"
url = "https://git.mydomain.com"

[default]
user = "wenqian"
email = "wenqian@mydomain.com"
sync = true
pin = true
ssh = true

[rename."torpedo"]
name = "cube"

[rename."kubernetes"]
name = "k8s"
repos."kubernetes" = "k8s"
repos."kubernetes-sigs" = "k8s-sigs"
